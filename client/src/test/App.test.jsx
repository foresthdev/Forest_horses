import '@testing-library/jest-dom/extend-expect';
import App from './App.jsx';

describe("Sopping cart functions", () => {
    test("Add item to the shopping cart", () => {
        
        

        expect(onAdd(item)).toEqual(27);
    })

    
})